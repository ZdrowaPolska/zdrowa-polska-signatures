# Zdrowa Polska email signatures

This repository contains only the public image assets and Apps Script source used for centrally managed Gmail signatures for `zdrowapolskagroup.pl`.

No tokens, service-account keys, or other secrets belong in this repository. Secrets are stored only in Apps Script Script Properties.

Public profile photos are stored under `data/photos/` because external email recipients must be able to load them in the signature.
